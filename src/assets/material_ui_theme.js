import { createMuiTheme } from '@material-ui/core/styles'

const theme = createMuiTheme({
    palette: {
        primary: {
          main: '#4587bb',
        },
        secondary: {
          main: '#ff879f',
        },
        // background: {
        //   paper: '#fff',
        //   default: '#eceef1'
        // },
      },
  });

export default theme